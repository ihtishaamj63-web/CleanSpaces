This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
backend/
  controllers/
    authController.js
    cleanupReportController.js
    contactController.js
    employeeController.js
    paymentController.js
    payrollController.js
    residentController.js
    testimonialController.js
    zoneController.js
  middleware/
    auth.js
  routes/
    admin.js
    auth.js
    cleanupReports.js
    contact.js
    employees.js
    payments.js
    payroll.js
    resident.js
    reviews.js
    stats.js
    testimonials.js
    zones.js
  .env.example
  db.js
  package.json
  server.js
frontend/
  src/
    assets/
      styles.css
    components/
      HeroSlider.vue
      StatsCounter.vue
      TestimonialCard.vue
      ZoneMap.vue
      ZoneRegisterForm.vue
    router/
      index.js
    views/
      admin/
        CleanupReports.vue
        Dashboard.vue
        Employees.vue
        Payroll.vue
        Zones.vue
      resident/
        Dashboard.vue
      About.vue
      Contact.vue
      Home.vue
      HowItWorks.vue
      Login.vue
      Payment.vue
      PaymentSuccess.vue
      Pricing.vue
      Reviews.vue
      Signup.vue
    api.js
    App.vue
    main.js
  index.html
  package.json
  vite.config.js
.gitignore
README.md
schema.sql
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="backend/controllers/authController.js">

</file>

<file path="backend/controllers/cleanupReportController.js">

</file>

<file path="backend/controllers/contactController.js">

</file>

<file path="backend/controllers/employeeController.js">

</file>

<file path="backend/controllers/payrollController.js">

</file>

<file path="backend/controllers/residentController.js">

</file>

<file path="backend/controllers/testimonialController.js">

</file>

<file path="backend/controllers/zoneController.js">

</file>

<file path="backend/routes/admin.js">
import { Router } from 'express'

const router = Router()

// Replace this stub with your real endpoints (see the dev plan for your list).

export default router
</file>

<file path="backend/routes/reviews.js">
import { Router } from 'express'

const router = Router()

// Replace this stub with your real endpoints (see the dev plan for your list).

export default router
</file>

<file path="backend/.env.example">

</file>

<file path="frontend/src/assets/styles.css">
/* ============ CleanSpaces shared design tokens ============ */
:root {
  /* Krish's theme palette (kept for any existing references) */
  --vt-c-white: #ffffff;
  --vt-c-white-soft: #f8f8f8;
  --vt-c-white-mute: #f2f2f2;
  --vt-c-black: #181818;
  --vt-c-black-soft: #222222;
  --vt-c-black-mute: #282828;
  --vt-c-indigo: #2c3e50;

  /* CleanSpaces palette */
  --green-dark: #12332d;       /* headers, navbar, dark cards */
  --green-deeper: #0b2a25;     /* text on green buttons, dark inputs */
  --green: #7cb342;            /* primary accent, buttons, links */
  --green-hover: #8bc34a;
  --green-tint: #edf5e3;       /* light green highlight backgrounds */
  --bg: #f4f6f5;               /* page background */
  --card: #ffffff;
  --text: #2e3d39;
  --text-muted: #6a7a76;       /* muted text on light backgrounds */
  --text-muted-dark: #a0b0ac;  /* muted text on dark green */
  --border: #e0e5e2;

  --color-background: var(--bg);
  --color-text: var(--text);
}

*,
*::before,
*::after { box-sizing: border-box; }

body {
  margin: 0;
  min-height: 100vh;
  background: var(--bg);
  color: var(--text);
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  font-size: 15px;
  line-height: 1.6;
  transition: color 0.5s, background-color 0.5s;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

img { max-width: 100%; }

button,
input,
select {
  font: inherit;
}

/* ============ Shared building blocks (scoped styles can override) ============ */
.section-heading { text-align: center; margin-bottom: 2rem; }
.section-heading h2 { margin: 0 0 .4rem; font-size: 2rem; color: var(--green-dark); }
.section-heading p { margin: 0; color: var(--text-muted); }

.card {
  background: var(--card);
  border-radius: 12px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, .05);
  padding: 1.75rem;
  margin-bottom: 1.5rem;
}

.btn-primary,
.btn-secondary {
  display: inline-block;
  padding: .85rem 1.6rem;
  border-radius: 8px;
  font-weight: 700;
  text-decoration: none;
}
.btn-primary { color: var(--green-deeper); background: var(--green); }
.btn-primary:hover { background: var(--green-hover); }
.btn-secondary { color: var(--green-dark); background: transparent; border: 2px solid var(--green-dark); }
</file>

<file path="frontend/src/api.js">
import axios from 'axios'

const api = axios.create({
  baseURL: '/api'
})

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

export default api
</file>

<file path=".gitignore">
node_modules/
.env
backend/.env
frontend/.env.local
frontend/dist/
</file>

<file path="schema.sql">

</file>

<file path="backend/controllers/paymentController.js">
import crypto from 'crypto'
import db from '../db.js'

// POST /api/payments/create
export async function createPayment(req, res) {
  try {
    // In production: get user from auth middleware (req.user.id).
    // During development (no auth yet), default to the resident test account.
    const userId = req.user ? req.user.id : 2

    const { zone_id, method } = req.body

    const [zones] = await db.query('SELECT * FROM zones WHERE id = ? AND status = ?', [zone_id, 'active'])
    if (zones.length === 0) {
      return res.status(400).json({ error: 'Zone not found or not active' })
    }
    const zone = zones[0]

    // Per-household share: midpoint of the plan range / households
    const planBase = { small: 4000, medium: 7250, large: 11500 }[zone.plan_type]
    const amount = Math.round(planBase / zone.households)

    // Ensure the user has a zone_members record
    const [membership] = await db.query(
      'SELECT * FROM zone_members WHERE user_id = ? AND zone_id = ?',
      [userId, zone_id]
    )
    if (membership.length === 0) {
      await db.query(
        'INSERT INTO zone_members (zone_id, user_id, payment_status) VALUES (?, ?, ?)',
        [zone_id, userId, 'pending']
      )
    }

    // Insert pending payment
    const [result] = await db.query(
      'INSERT INTO payments (user_id, zone_id, amount, method, status) VALUES (?, ?, ?, ?, ?)',
      [userId, zone_id, amount, method, 'pending']
    )
    const paymentId = result.insertId

    // DEV BYPASS — complete the payment immediately, no gateway
    if (process.env.DEV_BYPASS === 'true') {
      await db.query('UPDATE payments SET status = ? WHERE id = ?', ['completed', paymentId])
      await db.query(
        'UPDATE zone_members SET payment_status = ? WHERE user_id = ? AND zone_id = ?',
        ['paid', userId, zone_id]
      )
      const [rows] = await db.query('SELECT * FROM payments WHERE id = ?', [paymentId])
      return res.json({ bypass: true, payment: rows[0] })
    }

    // Real PayFast flow
    const params = buildPayfastParams(paymentId, amount, zone.name, req)

    res.json({ url: 'https://sandbox.payfast.co.za/eng/process', params })
  } catch (err) {
    console.error(err)
    res.status(500).json({ error: 'Failed to create payment' })
  }
}

// PayFast redirects the user back here — display result
export async function paymentReturn(req, res) {
  try {
    const [rows] = await db.query('SELECT * FROM payments WHERE id = ?', [req.params.id])
    if (rows.length === 0) return res.status(404).json({ error: 'Payment not found' })
    res.json(rows[0])
  } catch (err) {
    res.status(500).json({ error: 'Server error' })
  }
}

// PayFast's ITN (Instant Transaction Notification) webhook — the source of truth
export async function paymentNotify(req, res) {
  try {
    const data = req.body
    const paymentId = data.m_payment_id

    if (!verifyPayfastSignature(data)) {
      return res.status(400).json({ error: 'Invalid signature' })
    }

    if (data.payment_status === 'COMPLETE') {
      await db.query('UPDATE payments SET status = ? WHERE id = ?', ['completed', paymentId])
      const [payment] = await db.query('SELECT * FROM payments WHERE id = ?', [paymentId])
      if (payment.length > 0) {
        await db.query(
          'UPDATE zone_members SET payment_status = ? WHERE user_id = ? AND zone_id = ?',
          ['paid', payment[0].user_id, payment[0].zone_id]
        )
      }
    }
    res.sendStatus(200)
  } catch (err) {
    res.sendStatus(500)
  }
}

// --- PayFast helpers ---

function buildPayfastParams(paymentId, amount, zoneName, req) {
  const host = `${req.protocol}://${req.get('host')}`
  const params = {
    merchant_id: process.env.PAYFAST_MERCHANT_ID,
    merchant_key: process.env.PAYFAST_MERCHANT_KEY,
    return_url: `${host}/api/payments/return/${paymentId}`,
    cancel_url: `${host}/api/payments/return/${paymentId}`,
    notify_url: `${host}/api/payments/notify`,
    name_first: 'CleanSpaces',
    email_address: process.env.MAIL_USER,
    m_payment_id: String(paymentId),
    amount: amount.toFixed(2),
    item_name: `CleanSpaces subscription - ${zoneName}`
  }
  params.signature = generateSignature(params)
  return params
}

function generateSignature(params) {
  const passphrase = process.env.PAYFAST_PASSPHRASE || ''
  let data = Object.keys(params)
    .filter(k => params[k] !== '' && params[k] !== undefined)
    .sort()
    .map(k => `${k}=${encodeURIComponent(params[k])}`)
    .join('&')
  if (passphrase) data += `&passphrase=${encodeURIComponent(passphrase)}`
  return crypto.createHash('md5').update(data).digest('hex')
}

function verifyPayfastSignature(data) {
  const { signature, ...rest } = data
  return generateSignature(rest) === signature
}
</file>

<file path="backend/middleware/auth.js">
// Krishendree: replace this stub with real JWT verification (verify token, load user, check role).
// Keep the same default export so everyone's imports keep working when you swap it.

export default function auth(req, res, next) {
  req.user = null
  next()
}
</file>

<file path="backend/routes/auth.js">
import { Router } from 'express'

const router = Router()

// Replace this stub with your real endpoints (see the dev plan for your list).

export default router
</file>

<file path="backend/routes/cleanupReports.js">
import { Router } from 'express'

const router = Router()

// Replace this stub with your real endpoints (see the dev plan for your list).

export default router
</file>

<file path="backend/routes/contact.js">
import { Router } from 'express'

const router = Router()

// Replace this stub with your real endpoints (see the dev plan for your list).

export default router
</file>

<file path="backend/routes/employees.js">
import { Router } from 'express'

const router = Router()

// Replace this stub with your real endpoints (see the dev plan for your list).

export default router
</file>

<file path="backend/routes/payroll.js">
import { Router } from 'express'

const router = Router()

// Replace this stub with your real endpoints (see the dev plan for your list).

export default router
</file>

<file path="backend/routes/stats.js">
import { Router } from 'express'

const router = Router()

// Replace this stub with your real endpoints (see the dev plan for your list).

export default router
</file>

<file path="backend/routes/testimonials.js">
import { Router } from 'express'

const router = Router()

// Replace this stub with your real endpoints (see the dev plan for your list).

export default router
</file>

<file path="backend/routes/zones.js">
import { Router } from 'express'

const router = Router()

// Replace this stub with your real endpoints (see the dev plan for your list).

export default router
</file>

<file path="frontend/src/components/HeroSlider.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/components/StatsCounter.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/components/TestimonialCard.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/components/ZoneMap.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/components/ZoneRegisterForm.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/admin/CleanupReports.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/admin/Dashboard.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/admin/Employees.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/admin/Payroll.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/admin/Zones.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/resident/Dashboard.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/About.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/Contact.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/Home.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/HowItWorks.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/Login.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/Pricing.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/Reviews.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/src/views/Signup.vue">
<template>
  <div>
    <p>Page under construction.</p>
  </div>
</template>
</file>

<file path="frontend/index.html">
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="https://i.ibb.co/RpJFKCJX/cleanspaces-removebg-preview.png" />
    <title>CleanSpaces — Community-Powered Cleanup Service</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
</file>

<file path="frontend/vite.config.js">
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:5000'
    }
  }
})
</file>

<file path="README.md">
CleanSpaces
Community-powered cleanup service e-commerce platform.

Pages
Home | Pricing | About | Reviews | Contact | How It Works | Login/Signup | Payment | Resident Dashboard | Admin Panel

Setup
Clone the repo
Backend: cd backend → npm install → copy .env.example to .env and fill in your values → create the database and run schema.sql
Frontend: cd frontend → npm install
Run: backend npm run dev (port 5000), frontend npm run dev (port 5173)
Branches
main — stable version, protected
develop — integration branch, all pull requests go here
feature/* — personal working branches, created from develop
Test accounts
Admin: admin@cleanspaces.co.za / Admin@2026
Resident: thandiwe@gmail.com / Resident@2026
</file>

<file path="backend/routes/payments.js">
import { Router } from 'express'
import { createPayment, paymentReturn, paymentNotify } from '../controllers/paymentController.js'

const router = Router()

// Auth middleware will be added once Krishendree's is merged:
// import auth from '../middleware/auth.js'
// router.use(auth)

router.post('/create', createPayment)
router.get('/return/:id', paymentReturn)
router.post('/notify', paymentNotify)

export default router
</file>

<file path="backend/db.js">
import mysql from 'mysql2/promise'

const pool = mysql.createPool({
  host: process.env.DB_HOST || 'localhost',
  user: process.env.DB_USER || 'root',
  port: process.env.DB_PORT || 3306,
  password: process.env.DB_PASSWORD || '',
  database: process.env.DB_NAME || 'cleanspaces',
  waitForConnections: true,
  connectionLimit: 10
})

export default pool
</file>

<file path="backend/package.json">
{
  "name": "cleanspaces-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.19.0",
    "mysql2": "^3.9.0",
    "nodemailer": "^6.9.0"
  }
}
</file>

<file path="backend/server.js">
import 'dotenv/config'
import express from 'express'
import cors from 'cors'

import authRoutes from './routes/auth.js'
import statsRoutes from './routes/stats.js'
import paymentRoutes from './routes/payments.js'
import residentRoutes from './routes/resident.js'
import testimonialRoutes from './routes/testimonials.js'
import contactRoutes from './routes/contact.js'
import zoneRoutes from './routes/zones.js'
import employeeRoutes from './routes/employees.js'
import payrollRoutes from './routes/payroll.js'
import cleanupReportRoutes from './routes/cleanupReports.js'
import reviewRoutes from './routes/reviews.js'
import adminRoutes from './routes/admin.js'

const app = express()

app.use(cors())
app.use(express.json())
app.use(express.urlencoded({ extended: false })) // PayFast ITN arrives as form data, not JSON

app.get('/api/health', (req, res) => res.json({ status: 'ok' }))

app.use('/api/auth', authRoutes)
app.use('/api/stats', statsRoutes)
app.use('/api/payments', paymentRoutes)
app.use('/api/resident', residentRoutes)
app.use('/api/testimonials', testimonialRoutes)
app.use('/api/contact', contactRoutes)
app.use('/api/zones', zoneRoutes)
app.use('/api/employees', employeeRoutes)
app.use('/api/payroll', payrollRoutes)
app.use('/api/cleanup-reports', cleanupReportRoutes)
app.use('/api/reviews', reviewRoutes)
app.use('/api/admin', adminRoutes)

const PORT = process.env.PORT || 5000
app.listen(PORT, () => console.log(`CleanSpaces backend running on port ${PORT}`))
</file>

<file path="frontend/src/router/index.js">
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/', name: 'home', component: () => import('../views/Home.vue') },
    { path: '/pricing', name: 'pricing', component: () => import('../views/Pricing.vue') },
    { path: '/about', name: 'about', component: () => import('../views/About.vue') },
    { path: '/reviews', name: 'reviews', component: () => import('../views/Reviews.vue') },
    { path: '/contact', name: 'contact', component: () => import('../views/Contact.vue') },
    { path: '/how-it-works', name: 'howItWorks', component: () => import('../views/HowItWorks.vue') },
    { path: '/login', name: 'login', component: () => import('../views/Login.vue') },
    { path: '/signup', name: 'signup', component: () => import('../views/Signup.vue') },
    { path: '/payment', name: 'payment', component: () => import('../views/Payment.vue') },
    { path: '/payment/success/:id', name: 'paymentSuccess', component: () => import('../views/PaymentSuccess.vue') },
    { path: '/resident', redirect: '/resident/dashboard' },
    { path: '/resident/dashboard', name: 'residentDashboard', component: () => import('../views/resident/Dashboard.vue'), meta: { requiresAuth: true } },
    { path: '/admin/dashboard', name: 'adminDashboard', component: () => import('../views/admin/Dashboard.vue'), meta: { requiresAuth: true, requiresAdmin: true } },
    { path: '/admin/zones', name: 'adminZones', component: () => import('../views/admin/Zones.vue'), meta: { requiresAuth: true, requiresAdmin: true } },
    { path: '/admin/employees', name: 'adminEmployees', component: () => import('../views/admin/Employees.vue'), meta: { requiresAuth: true, requiresAdmin: true } },
    { path: '/admin/payroll', name: 'adminPayroll', component: () => import('../views/admin/Payroll.vue'), meta: { requiresAuth: true, requiresAdmin: true } },
    { path: '/admin/cleanup-reports', name: 'adminCleanupReports', component: () => import('../views/admin/CleanupReports.vue'), meta: { requiresAuth: true, requiresAdmin: true } },
    { path: '/:pathMatch(.*)*', redirect: '/' }
  ]
})

router.beforeEach((to) => {
  const token = localStorage.getItem('token')
  const role = localStorage.getItem('role')
  if (to.meta.requiresAuth && !token) return { name: 'login' }
  if (to.meta.requiresAdmin && role !== 'admin') return { name: 'home' }
})

export default router
</file>

<file path="frontend/src/main.js">
import { createApp } from 'vue'
import App from './App.vue'
import router from './router/index.js'
import './assets/styles.css'

createApp(App).use(router).mount('#app')
</file>

<file path="frontend/src/views/Payment.vue">
<template>
  <div class="payment-page">
    <section class="payment-wrap">
      <div class="section-heading">
        <h1>Complete Your Subscription</h1>
        <p>Choose your zone and pay your monthly contribution.</p>
      </div>

      <div class="steps">
        <span class="step done">1. Review</span>
        <span class="step current">2. Payment</span>
        <span class="step">3. Confirmation</span>
      </div>

      <div class="card">
        <h3>Order Summary</h3>
        <div class="input-group">
          <label for="zone">Your Zone</label>
          <select id="zone" v-model="selectedZoneId" class="select">
            <option v-for="z in zones" :key="z.id" :value="z.id">
              {{ z.name }} — {{ z.neighborhood }} ({{ z.households }} households)
            </option>
          </select>
        </div>

        <div v-if="selectedZone" class="summary">
          <div class="row"><span>Zone</span><span>{{ selectedZone.name }}</span></div>
          <div class="row"><span>Plan</span><span>{{ planInfo.label }}</span></div>
          <div class="row"><span>Monthly zone total</span><span>{{ planInfo.range }}</span></div>
          <div class="row"><span>Your household share</span><span>± R{{ perHousehold }}</span></div>
        </div>
      </div>

      <div class="card">
        <h3>Payment Method</h3>
        <div class="methods">
          <label class="method" :class="{ active: method === 'card' }">
            <input type="radio" value="card" v-model="method" /> 💳 Card
          </label>
          <label class="method" :class="{ active: method === 'eft' }">
            <input type="radio" value="eft" v-model="method" /> 🏦 EFT
          </label>
        </div>
        <p class="note">You will complete the payment on PayFast's secure page. Card details are never entered on our site.</p>

        <p v-if="error" class="error-text">{{ error }}</p>

        <button class="submit-btn" type="button" :disabled="loading || !selectedZoneId" @click="pay">
          {{ loading ? 'Processing…' : 'Proceed to Secure Payment' }}
        </button>
        <p class="secure">🔒 Payments processed securely by PayFast (PCI DSS Level 1 certified)</p>
      </div>
    </section>
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import api from '../api.js'

const router = useRouter()

const zones = ref([])
const selectedZoneId = ref(null)
const method = ref('card')
const loading = ref(false)
const error = ref('')

// Same fallback zones as the ZoneMap so demo data is consistent site-wide
const fallbackZones = [
  { id: 1, name: 'NY108 Block', neighborhood: 'Manenberg', households: 62, plan_type: 'small', status: 'active' },
  { id: 3, name: 'Tafelsig', neighborhood: "Mitchell's Plain", households: 180, plan_type: 'medium', status: 'active' },
  { id: 5, name: 'Site C', neighborhood: 'Khayelitsha', households: 210, plan_type: 'large', status: 'active' },
]

const planInfoMap = {
  small: { label: 'Small Zone', range: 'R3,500 – R4,500' },
  medium: { label: 'Medium Zone', range: 'R6,500 – R8,000' },
  large: { label: 'Large Zone', range: 'R10,000 – R13,000' },
}

const selectedZone = computed(() => zones.value.find(z => z.id === selectedZoneId.value) || null)
const planInfo = computed(() => planInfoMap[selectedZone.value?.plan_type] || { label: '—', range: '—' })
const perHousehold = computed(() => {
  if (!selectedZone.value) return '—'
  const base = { small: 4000, medium: 7250, large: 11500 }[selectedZone.value.plan_type] || 0
  return Math.round(base / selectedZone.value.households)
})

onMounted(async () => {
  try {
    const res = await api.get('/zones')
    const active = (Array.isArray(res.data) ? res.data : []).filter(z => z.status === 'active')
    if (active.length === 0) throw new Error('no active zones')
    zones.value = active
  } catch {
    zones.value = fallbackZones
  }
  selectedZoneId.value = zones.value[0]?.id ?? null
})

async function pay() {
  loading.value = true
  error.value = ''
  try {
    const res = await api.post('/payments/create', {
      zone_id: selectedZoneId.value,
      method: method.value
    })
    if (res.data.bypass) {
      router.push(`/payment/success/${res.data.payment.id}`)
    } else {
      submitToPayfast(res.data.url, res.data.params)
    }
  } catch {
    error.value = 'Could not start the payment. Is the backend running?'
    loading.value = false
  }
}

// PayFast requires a form POST with the signed parameters from our backend
function submitToPayfast(url, params) {
  const form = document.createElement('form')
  form.method = 'POST'
  form.action = url
  Object.keys(params).forEach((key) => {
    const input = document.createElement('input')
    input.type = 'hidden'
    input.name = key
    input.value = params[key]
    form.appendChild(input)
  })
  document.body.appendChild(form)
  form.submit()
}
</script>

<style scoped>
.payment-page { min-height: 100vh; padding: 3rem 1rem; background: #f4f6f5; }
.payment-wrap { max-width: 900px; margin: 0 auto; }
.section-heading { text-align: center; margin-bottom: 1.5rem; }
.section-heading h1 { margin: 0 0 .4rem; font-size: 2rem; color: #12332d; }
.section-heading p { margin: 0; color: #6a7a76; }

.steps { display: flex; justify-content: center; gap: .5rem; flex-wrap: wrap; margin-bottom: 2rem; }
.step { padding: .3rem .9rem; border-radius: 20px; font-size: .8rem; font-weight: 700; color: #6a7a76; background: white; border: 1px solid #e0e5e2; }
.step.done { color: #0b2a25; background: #edf5e3; border-color: #7cb342; }
.step.current { color: #0b2a25; background: #7cb342; border-color: #7cb342; }

.card { background: white; border-radius: 12px; box-shadow: 0 5px 15px rgba(0,0,0,.05); padding: 1.75rem; margin-bottom: 1.5rem; }
.card h3 { margin: 0 0 1.2rem; color: #12332d; }

.input-group { display: grid; gap: .4rem; margin-bottom: 1rem; }
.input-group label { font-size: .88rem; font-weight: 600; color: #12332d; }
.select { padding: .8rem 1rem; background: #f4f8f5; border: 1px solid #e0e5e2; border-radius: 8px; font-size: 1rem; color: #12332d; }
.select:focus { outline: 2px solid #7cb342; outline-offset: 2px; }

.summary .row { display: flex; justify-content: space-between; padding: .75rem 0; border-bottom: 1px solid #f0f0f0; color: #2e3d39; }
.summary .row span:last-child { font-weight: 700; color: #12332d; }

.methods { display: flex; gap: 1rem; flex-wrap: wrap; margin-bottom: 1rem; }
.method { padding: .8rem 1.5rem; background: #f4f8f5; border: 1px solid #e0e5e2; border-radius: 8px; font-weight: 600; color: #12332d; cursor: pointer; }
.method.active { background: #edf5e3; border: 2px solid #7cb342; }
.method input { margin-right: .5rem; accent-color: #7cb342; }

.note { margin: 0 0 1.5rem; color: #6a7a76; font-size: .9rem; }
.error-text { margin: 0 0 1rem; color: #d32f2f; font-size: .9rem; }

.submit-btn { width: 100%; padding: 1rem; color: #0b2a25; background: #7cb342; border: 0; border-radius: 8px; font-weight: 700; font-size: 1.05rem; cursor: pointer; transition: all .3s ease; }
.submit-btn:hover { background: #8bc34a; transform: translateY(-2px); }
.submit-btn:disabled { opacity: .6; cursor: wait; transform: none; }
.secure { margin: 1rem 0 0; text-align: center; color: #6a7a76; font-size: .85rem; }

@media (max-width: 600px) {
  .section-heading h1 { font-size: 1.6rem; }
  .methods { flex-direction: column; }
  .method { text-align: center; }
}
</style>
</file>

<file path="frontend/src/views/PaymentSuccess.vue">
<template>
  <div class="auth-page">
    <div class="auth-card">
      <div class="logo-section">
        <img src="https://i.ibb.co/RpJFKCJX/cleanspaces-removebg-preview.png" alt="CleanSpaces Logo" class="logo" />
        <h1 class="brand-title">CLEAN<span>SPACES</span></h1>
        <p class="tagline">Cleaner Spaces. Stronger Communities.</p>
      </div>

      <div v-if="payment" class="confirmation">
        <div class="check">✓</div>
        <h2 class="confirm-title">Payment Successful</h2>
        <p class="confirm-text">Thank you! Your contribution to your zone has been received.</p>

        <div class="details">
          <div class="row"><span>Reference</span><span>#CS-{{ String(payment.id).padStart(5, '0') }}</span></div>
          <div class="row"><span>Amount</span><span>R{{ payment.amount }}</span></div>
          <div class="row"><span>Method</span><span>{{ payment.method }}</span></div>
        </div>

        <router-link to="/resident/dashboard" class="submit-btn">Go to My Dashboard</router-link>
      </div>
      <p v-else class="loading-text">Loading your payment…</p>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import api from '../api.js'

const route = useRoute()
const payment = ref(null)

onMounted(async () => {
  try {
    const res = await api.get(`/payments/return/${route.params.id}`)
    payment.value = res.data
  } catch {
    payment.value = { id: route.params.id, amount: '—', method: '—' }
  }
})
</script>

<style scoped>
.auth-page { min-height: 100vh; display: grid; place-items: center; padding: 2rem 1rem; background: #f4f6f5; }
.auth-card { width: min(100%, 440px); padding: 2.5rem; background: #12332d; color: #f4f6f5; border-radius: 16px; box-shadow: 0 15px 35px rgba(0,0,0,.35); text-align: center; }

.logo-section { margin-bottom: 1.75rem; }
.logo { width: 80px; height: 80px; object-fit: contain; margin-bottom: .5rem; background: white; padding: 5px; border-radius: 50%; }
.brand-title { margin: 0 0 .3rem; font-size: 1.8rem; font-weight: 800; text-transform: uppercase; }
.brand-title span { color: #7cb342; }
.tagline { margin: 0; color: #a0b0ac; font-size: .9rem; }

.check { width: 64px; height: 64px; line-height: 64px; border-radius: 50%; background: #7cb342; color: #0b2a25; font-size: 32px; font-weight: 800; margin: 0 auto 1rem; }
.confirm-title { margin: 0 0 .4rem; font-size: 1.4rem; }
.confirm-text { margin: 0 0 1.5rem; color: #a0b0ac; font-size: .95rem; }

.details { text-align: left; margin: 0 0 1.5rem; }
.details .row { display: flex; justify-content: space-between; padding: .7rem 0; border-bottom: 1px solid rgba(255,255,255,.12); }
.details .row span:first-child { color: #a0b0ac; }
.details .row span:last-child { font-weight: 700; }

.submit-btn { display: block; width: 100%; padding: .9rem; color: #0b2a25; background: #7cb342; border: 0; border-radius: 8px; font-weight: 700; font-size: 1rem; cursor: pointer; text-decoration: none; }
.submit-btn:hover { background: #8bc34a; }
.loading-text { color: #a0b0ac; }
</style>
</file>

<file path="frontend/src/App.vue">
<template>
  <header class="site-header">
    <div class="header-inner">
      <router-link to="/" class="logo-brand">
        <img src="https://i.ibb.co/RpJFKCJX/cleanspaces-removebg-preview.png" alt="CleanSpaces Logo" class="logo" />
        <span class="brand-title">CLEAN<span>SPACES</span></span>
      </router-link>

      <nav class="nav-links">
        <router-link to="/pricing">Pricing</router-link>
        <router-link to="/how-it-works">How It Works</router-link>
        <router-link to="/reviews">Reviews</router-link>
        <router-link to="/about">About</router-link>
        <router-link to="/contact">Contact</router-link>
      </nav>

      <div class="header-actions">
        <router-link v-if="role === 'admin'" to="/admin/dashboard" class="action-btn">Admin</router-link>
        <router-link v-else-if="token" to="/resident/dashboard" class="action-btn">My Dashboard</router-link>
        <router-link v-else to="/login" class="action-btn">Log In</router-link>
        <a v-if="token" href="#" class="action-btn outline" @click.prevent="logout">Log Out</a>
      </div>
    </div>
  </header>

  <main>
    <router-view />
  </main>

  <footer class="site-footer">
    <p>CleanSpaces — Community-Powered Cleanup Service</p>
  </footer>
</template>

<script setup>
import { ref } from 'vue'
import { useRouter } from 'vue-router'

const router = useRouter()
const token = ref(localStorage.getItem('token'))
const role = ref(localStorage.getItem('role'))

function logout() {
  localStorage.removeItem('token')
  localStorage.removeItem('role')
  token.value = null
  role.value = null
  router.push('/')
}
</script>

<style scoped>
.site-header { color: #f4f6f5; background: #12332d; position: sticky; top: 0; z-index: 100; box-shadow: 0 4px 15px rgba(0,0,0,.15); }
.header-inner { display: flex; align-items: center; justify-content: space-between; gap: 1.5rem; max-width: 1200px; margin: 0 auto; padding: .8rem 1.5rem; flex-wrap: wrap; }
.logo-brand { display: flex; align-items: center; gap: .75rem; text-decoration: none; }
.logo { width: 44px; height: 44px; object-fit: contain; background: white; padding: 3px; border-radius: 50%; }
.brand-title { font-size: 1.25rem; font-weight: 800; letter-spacing: .5px; color: #f4f6f5; text-transform: uppercase; }
.brand-title span { color: #7cb342; }
.nav-links { display: flex; gap: 1.25rem; flex-wrap: wrap; }
.nav-links a { color: #a0b0ac; text-decoration: none; font-size: .95rem; font-weight: 500; }
.nav-links a:hover, .nav-links a.router-link-active { color: #7cb342; }
.header-actions { display: flex; gap: .75rem; }
.action-btn { padding: .5rem 1.1rem; color: #7cb342; background: transparent; border: 2px solid #7cb342; border-radius: 8px; font-weight: 700; font-size: .9rem; text-decoration: none; }
.action-btn:hover { color: #0b2a25; background: #7cb342; }
.action-btn.outline { color: #a0b0ac; border-color: #a0b0ac; }
.action-btn.outline:hover { color: #12332d; background: #a0b0ac; }
.site-footer { background: #12332d; color: #a0b0ac; padding: 1.5rem 1rem; text-align: center; }
.site-footer p { margin: .2rem 0; font-size: .9rem; }
.footer-muted { font-size: .8rem; }
</style>
</file>

<file path="frontend/package.json">
{
  "name": "cleanspaces-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "leaflet": "^1.9.4",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.2.0"
  }
}
</file>

<file path="backend/routes/resident.js">
import { Router } from 'express'
import db from '../db.js'

const router = Router()

router.get('/dashboard', async (req, res) => {
  try {
    const userId = req.user ? req.user.id : 2

    const [member] = await db.query(
      `SELECT zm.*, z.name AS zone_name, z.plan_type, z.households, z.neighborhood
       FROM zone_members zm JOIN zones z ON z.id = zm.zone_id
       WHERE zm.user_id = ?`, [userId])

    if (member.length === 0) return res.json({ hasZone: false })

    const zone = member[0]
    const [paid] = await db.query(
      "SELECT COUNT(*) AS c FROM zone_members WHERE zone_id = ? AND payment_status = 'paid'", [zone.zone_id])

    const threshold = Math.ceil(zone.households * 0.6)

    res.json({
      hasZone: true,
      zone: {
        name: zone.zone_name,
        neighborhood: zone.neighborhood,
        plan: zone.plan_type,
        households: zone.households,
        paid: paid[0].c,
        threshold,
        myStatus: zone.payment_status
      }
    })
  } catch (err) {
    console.error('DASHBOARD ERROR:', err)
    res.status(500).json({ error: 'Server error' })
  }
})

router.get('/payments', async (req, res) => {
  try {
    const userId = req.user ? req.user.id : 2
    const [rows] = await db.query(
      'SELECT * FROM payments WHERE user_id = ? ORDER BY created_at DESC', [userId])
    res.json(rows)
  } catch (err) {
    res.status(500).json({ error: 'Server error' })
  }
})

router.get('/cleanups', async (req, res) => {
  try {
    const userId = req.user ? req.user.id : 2
    const [rows] = await db.query(
      `SELECT cr.* FROM cleanup_reports cr
       JOIN zone_members zm ON zm.zone_id = cr.zone_id
       WHERE zm.user_id = ? ORDER BY cr.date_cleaned DESC`, [userId])
    res.json(rows)
  } catch {
    res.status(500).json({ error: 'Server error' })
  }
})

export default router
</file>

</files>
